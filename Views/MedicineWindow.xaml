<Window x:Class="AyurvedaClinicManagementSystem.Views.MedicineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Medicine Management"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form -->
        <StackPanel Grid.Row="0">

            <TextBlock Text="Medicine Name"/>
            <TextBox x:Name="txtMedicineName" Height="30" Margin="0,5"/>

            <TextBlock Text="Category"/>
            <TextBox x:Name="txtCategory" Height="30" Margin="0,5"/>

            <TextBlock Text="Quantity"/>
            <TextBox x:Name="txtQuantity" Height="30" Margin="0,5"/>

            <TextBlock Text="Price"/>
            <TextBox x:Name="txtPrice" Height="30" Margin="0,5"/>

            <TextBlock Text="Expiry Date"/>
            <DatePicker x:Name="dpExpiryDate" Margin="0,5"/>

            <TextBlock Text="Search"/>
            <TextBox x:Name="txtSearch"
                     Height="30"
                     Margin="0,5"
                     TextChanged="txtSearch_TextChanged"/>

            <StackPanel Orientation="Horizontal" Margin="0,10">

                <Button Content="Save"
                        Width="100"
                        Margin="5"
                        Background="Green"
                        Foreground="White"
                        Click="Save_Click"/>

                <Button Content="Update"
                        Width="100"
                        Margin="5"
                        Background="Orange"
                        Foreground="White"
                        Click="Update_Click"/>

                <Button Content="Delete"
                        Width="100"
                        Margin="5"
                        Background="Red"
                        Foreground="White"
                        Click="Delete_Click"/>

            </StackPanel>

        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="MedicineGrid"
                  Grid.Row="1"
                  Margin="5"
                  AutoGenerateColumns="True"
                  SelectionChanged="MedicineGrid_SelectionChanged"/>

    </Grid>
</Window>